import { h, Component } from "preact";
import Circle from './Circle';

const circlePos = 

const ShapePicker = ({shape, x, y, width}) => {
    switch (shape) {
        case "circle":
            
            break;
    
        default:
            break;
    }
}

export default ShapePicker;